# API密钥
DASHSCOPE_API_KEY=

# MySQL数据库配置
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=root
MYSQL_DATABASE=langchain_chat

# MongoDB数据库配置
MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_USER=
MONGODB_PASSWORD=
MONGODB_DATABASE=langchain_chat

# ─── Redis 配置 ───────────────────────────────────────────────
# 本地默认安装无需修改，如有密码填写 REDIS_PASSWORD
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_CHAT_CACHE_TTL=3600        # 会话历史缓存过期时间（秒）
REDIS_TOKEN_BLACKLIST_TTL=604800 # Token 黑名单 TTL（7天，与 JWT 有效期一致）

# ─── RAG 配置 ─────────────────────────────────────────────────
RAG_VECTOR_STORE_DIR=vector_stores  # FAISS 向量库本地存储目录（相对 backend/）
RAG_CHUNK_SIZE=500                  # 文本切片大小（字符数）
RAG_CHUNK_OVERLAP=50               # 相邻切片重叠字符数
RAG_TOP_K=3                         # 每次检索返回最相关片段数

# JWT密钥
SECRET_KEY=your-secret-key-change-this-in-production


# 调试模式
DEBUG=True